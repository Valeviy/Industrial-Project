<Page x:Class="BCG_UI.Page1"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BCG_UI"
      xmlns:view="clr-namespace:BCG_UI.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <ListView ItemsSource="{Binding Resources}"
                  SelectedItem="{Binding SelectedResource,Mode=TwoWay}"
                  DisplayMemberPath="ResourceName"/>
        <Grid Grid.Column="1">

            <view:ResourceView DataContext="{Binding ResourceViewModel}"></view:ResourceView>
        </Grid>

        <view:ResourceDetailedView Grid.Column="2" DataContext="{Binding ResourcesDetailedViewModel}"/>
    </Grid>
</Page>
